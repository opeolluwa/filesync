use glob::glob;
extern crate glob;
use walkdir::WalkDir;

fn main() {
  /*   for entry in WalkDir::new("foo") {
        let entry = entry.unwrap();
        println!("{}", entry.path().display());
    } */
    println("hey fool");
}
