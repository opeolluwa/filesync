{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "os:default",
    "sql:allow-load",
    "sql:allow-execute",
    "sql:allow-select",
    "sql:default",
    "sql:allow-close"
  ]
}
